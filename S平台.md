# SPADE 平台資料

| | 內容 | 什麼時候用 |
|---|---|---|
| **Part A** | 給 SPADE 的提示詞 | 貼給 SPADE 執行。它會自己探測、自己填規格表 |
| **Part B** | SPADE 填出來的規格表 | **以後每次要我做 SPADE 相關工作，把這份貼給我** |

Part A 跑完就會直接產出 Part B，你只要核對「推測」那幾項。

**要跑兩次**——第一次埋標記，第二次才測得出檔案有沒有保留，而那題會決定整個架構。

---
---

# Part A：給 SPADE 的提示詞

複製「開始」到「結束」之間整段，貼進 SPADE。

## ── 開始複製 ──

我需要一份你這個平台的完整能力規格，用來決定之後要在上面開發的工具該怎麼設計。

請分兩階段做：**先實際探測，再填規格表**。

---

## 最重要的規則：每一項答案都要標來源

這份規格會被拿去做設計決策，**答錯比答「不知道」的代價高得多**。所以每一項都必須標上下面四種標籤之一：

| 標籤 | 意思 |
|---|---|
| `【實測】` | 我寫程式跑出來的，並附上實際輸出當證據 |
| `【文件】` | 我的系統指示或平台文件裡明確寫的 |
| `【推測】` | 我根據一般經驗推斷的，**沒有驗證過** |
| `【不知道】` | 查不到也測不出來 |

**硬性要求：**

- **能測的一律去測，不要用推測代替。** 例如「檔案會不會保留」請實際寫檔案驗證，不要憑印象回答
- **寧可寫【不知道】，也不要寫【推測】。** 我寧願缺一格，也不要一格看起來很確定但其實是錯的
- 所有標 `【推測】` 的項目，請在最後**集中列成一張清單**，標題寫「以下需人工確認」
- 不要為了讓表格看起來完整而填滿

---

## 階段一：實際探測

寫一支 Python 腳本並執行。限制：

1. **只用標準函式庫**，探測第三方套件時用 try/except 包起來
2. **絕對不要印出任何環境變數的「值」**，只能印「名稱」——那裡面可能有金鑰或密碼
3. **不要連任何外部網路**
4. 每項用 try/except 包住，某項失敗就印錯誤類型並繼續，不要整支中斷
5. 只在暫存目錄寫測試檔，寫完刪掉（持久性測試的那個檔例外）

要探測的項目：

**1. Python 與編碼**
版本、直譯器路徑、作業系統、CPU 架構、`sys.getdefaultencoding()`、`sys.getfilesystemencoding()`、`locale.getpreferredencoding()`、`sys.stdout.encoding`、`sys.stdout` 有無 `reconfigure`。
印出這兩行測試中文顯示：
`第五條 要保人應於保險費到期日前繳納保險費。`
`（一）　○○　臺灣　鍾～彭　①②`

**2. 標準函式庫有沒有被移除**
逐一 import 並印有／無：`subprocess`、`socket`、`ctypes`、`multiprocessing`、`threading`、`sqlite3`、`zipfile`、`tarfile`、`xml.etree.ElementTree`、`csv`、`json`、`hashlib`、`urllib.request`、`shutil`、`tempfile`、`pathlib`、`unicodedata`

**3. 第三方套件**
逐一 import，印有／無與版本，分組列出：
- 文件：`docx`、`openpyxl`、`xlsxwriter`、`pypdf`、`fitz`、`pdfplumber`、`bs4`、`lxml`、`markdown`
- 資料：`pandas`、`numpy`、`yaml`、`chardet`、`charset_normalizer`、`tabulate`
- 文字比對／中文：`rapidfuzz`、`Levenshtein`、`regex`、`jieba`、`opencc`
- 機器學習：`sklearn`、`torch`、`transformers`、`sentence_transformers`
- 其他：`requests`、`httpx`、`PIL`、`matplotlib`、`jinja2`、`dateutil`
統計可用幾個。另外檢查 `pip` 模組存不存在。

**4. 檔案系統與持久性（最重要）**
- 印出目前工作目錄、暫存目錄、家目錄的絕對路徑
- 測試三個位置能否寫入（寫小檔再刪）
- 中文檔名測試：建「測試_中文檔名.txt」後刪除
- 長檔名測試：建一個 150 字元檔名的檔案
- **持久性測試**：檢查暫存目錄有沒有 `_probe_persist.txt`
  - 有 → 印「找到上次留下的檔案，內容：<內容>」
  - 沒有 → 印「這是第一次執行，要再跑一次才知道」
  - 不論有無，都把目前時間寫進去，**這個檔不要刪**
- 列出目前工作目錄下的檔案與資料夾（最多 30 個）

**5. 執行能力**
- `subprocess` 能不能開子行程？跑 `python -c "print('ok')"` 試試
- 能不能 import 並執行同目錄下另一個 .py 檔？（測完刪掉）
- 輸出長度：連續印 `[1]` 到 `[500]`（每 25 個換行），最後印「若看得到 [500] 代表未被截斷」

**6. 執行時間上限**
跑一個迴圈每秒印一次「已經過 N 秒」，印到 30 秒為止，最後印實際總秒數。
輸出若停在中途，那就是平台的時間上限。

**7. 平台線索**
- 環境變數總數
- 名稱含 SPADE、PLATFORM、AGENT、SANDBOX、WORKSPACE、SKILL、RUNTIME、CONTAINER、SESSION 字樣的變數，**只印名稱不印值**，最多 25 個
- 目前使用者名稱與行程 ID

**請把腳本的原始輸出完整貼出來**，放在程式碼區塊裡，不要只給摘要。

---

## 階段二：填寫規格表

用階段一的結果，加上你從系統指示或平台文件知道的事，把下面填好。
**每一格開頭都要有標籤**（`【實測】`／`【文件】`／`【推測】`／`【不知道】`）。

```
=========== SPADE 平台規格表 ===========
填寫日期：

--- B1 執行環境 ---
Python 版本            ：
作業系統               ：
stdout 編碼            ：
中文顯示是否正常        ：
被移除的標準函式庫      ：

--- B2 套件 ---
可用的第三方套件        ：
能不能自行安裝套件      ：
若能，走什麼管道        ：

--- B3 檔案系統 ---
使用者上傳的檔案放哪    ：
產出檔案該寫到哪        ：
可寫入的位置            ：
執行之間檔案保留嗎      ：   ← 這題請用實測回答，不要用推測
中文檔名可用嗎          ：

--- B4 執行限制 ---
單次執行時間上限        ：
stdout 會不會被截斷      ：
subprocess 可用嗎        ：
記憶體限制              ：
能否跑背景／長時間任務   ：

--- B5 網路 ---
能連外網嗎              ：
若限白名單，開放哪些     ：
內部服務可以連嗎        ：

--- B6 Skill 機制 ---
Skill 定義檔格式        ：
Skill 放在哪            ：
資料夾命名規則          ：
Skill 怎麼被觸發        ：
檔案數量或大小上限      ：
更新 skill 要重啟嗎      ：

--- B7 對話與 I/O ---
使用者怎麼給輸入        ：
產出怎麼回到使用者      ：
使用者進得去產出資料夾嗎 ：
你看得到腳本完整輸出嗎   ：
對話長度上限            ：

--- B8 合規與資安 ---
可以處理公司內部文件嗎   ：
不能寫入的敏感目錄      ：
產出檔案留存多久        ：
有無稽核紀錄要求        ：
對外連線需要報備嗎      ：

--- 以下需人工確認 ---
（把所有標【推測】的項目集中列在這裡）
```

**最後再檢查一次**：有沒有哪一格其實是你猜的、卻標成了【文件】或【實測】？有的話改回【推測】並移到最後那張清單。

## ── 結束複製 ──

---
---

# Part B：SPADE 填出來的規格表

（把 SPADE 產出的規格表貼在這裡保存）

```
（待填）
```

---

## 拿到之後你要做的事

**只需核對兩種標籤：**

| 標籤 | 你要做什麼 |
|---|---|
| `【實測】` | 不用管，有證據 |
| `【文件】` | 大致可信，但 B6、B7 建議跟資訊單位對一次 |
| **`【推測】`** | **一定要人工確認**，這是 AI 猜的 |
| `【不知道】` | 你補，或標記為「先不依賴這一項」 |

**特別注意 B6、B7 這兩區。** SPADE 對「自己的 skill 怎麼載入」「產出怎麼給使用者」很可能是從系統指示推斷的，而 AI 描述自己的運作方式時最容易講得篤定卻講錯。這兩區若標成【文件】，最好還是找人核對。

## 我拿到 Part B 之後會做什麼

| 規格內容 | 我的設計會怎麼變 |
|---|---|
| 檔案不保留 | 改成單次執行完成所有步驟，不產中間檔 |
| 缺某些套件 | 用標準函式庫自己實作，或明確標示該功能不可用 |
| subprocess 被擋 | 改成單一入口，不讓腳本互相呼叫 |
| stdout 會截斷 | 報表寫檔，對話只放摘要 |
| 使用者拿不到檔案 | 該決定的內容全部寫進對話，並訂出分界 |
| 執行時間上限很短 | 拆步驟、加進度輸出，或換更快的演算法 |
| 不能連外網 | 排除所有需下載模型或查外部資料的功能 |

標【推測】或【不知道】的項目，**我會當成未知處理**——保守設計，或動工前先問你。不會拿沒驗證過的假設去蓋架構。
