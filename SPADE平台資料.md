# SPADE 平台資料

這份文件有兩部分，用途不同：

| | 內容 | 什麼時候用 |
|---|---|---|
| **Part A** | 探測提示詞 | 貼給 SPADE 執行一次。平台改版後重跑 |
| **Part B** | 平台規格表 | 依 Part A 的結果 ＋ 你自己知道的事填好。**以後每次要我做 SPADE 相關工作，把填好的 Part B 貼給我** |

Part B 才是要長期保留的。Part A 只是用來把它填出來。

---
---

# Part A：探測提示詞

複製「開始」到「結束」之間的整段，貼進 SPADE 執行。

**要跑兩次**——第一次埋標記，第二次才看得出檔案有沒有保留，而那題決定架構。

## ── 開始複製 ──

請幫我寫一支 Python 腳本並執行，探測你目前的執行環境有哪些能力。我要用這份報告決定之後在這個平台上開發工具的設計方式。

**硬性限制，務必遵守：**

1. **只用 Python 標準函式庫**。探測「有沒有某套件」時用 try/except 包起來
2. **絕對不要印出任何環境變數的「值」**，只能印「名稱」。那裡面可能有金鑰或密碼，印出來會留在對話紀錄
3. **不要連任何外部網路**，不要發 HTTP 請求
4. 每項檢查都用 try/except 包住，某項失敗就印錯誤類型並繼續，不要整支中斷
5. 只在暫存目錄寫測試檔，寫完刪掉（唯一例外見第 4 類）

**請依序檢查這七類，每類印出清楚標題：**

**第 1 類：Python 與編碼**
- Python 版本、直譯器路徑、作業系統與版本、CPU 架構
- `sys.getdefaultencoding()`、`sys.getfilesystemencoding()`、`locale.getpreferredencoding()`
- `sys.stdout.encoding`，以及 `sys.stdout` 有沒有 `reconfigure` 方法
- 印出這行中文測試字串確認會不會亂碼：`第五條 要保人應於保險費到期日前繳納保險費。`
- 印出這行含全形符號與罕用字：`（一）　○○　臺灣　鍾～彭　①②`

**第 2 類：標準函式庫有沒有被閹割**
有些沙箱會移除部分標準模組。逐一 import 並印出「有／無」：
`subprocess`、`socket`、`ctypes`、`multiprocessing`、`threading`、`sqlite3`、`zipfile`、`tarfile`、`xml.etree.ElementTree`、`csv`、`json`、`hashlib`、`urllib.request`、`http.client`、`shutil`、`tempfile`、`pathlib`、`unicodedata`、`decimal`

**第 3 類：第三方套件**
逐一嘗試 import，印「有／無」與版本號，依類別分組印出：
- 文件：`docx`、`openpyxl`、`xlsxwriter`、`pypdf`、`fitz`、`pdfplumber`、`bs4`、`lxml`、`markdown`
- 資料：`pandas`、`numpy`、`yaml`、`chardet`、`charset_normalizer`、`tabulate`
- 文字比對／中文：`rapidfuzz`、`Levenshtein`、`regex`、`jieba`、`opencc`、`zhon`
- 機器學習：`sklearn`、`torch`、`transformers`、`sentence_transformers`
- 網路／其他：`requests`、`httpx`、`PIL`、`matplotlib`、`jinja2`、`dateutil`、`pytz`
最後統計可用幾個。另外檢查 `pip` 模組存不存在。

**第 4 類：檔案系統與持久性（最重要）**
- 印出目前工作目錄、暫存目錄、使用者家目錄的**絕對路徑**
- 測試上述三個位置能不能寫入（寫小檔再刪），逐一印結果
- 中文檔名測試：在暫存目錄建「測試_中文檔名.txt」，確認成功後刪除
- 長檔名測試：建一個檔名長度 150 字元的檔案，看會不會失敗
- **持久性測試**：檢查暫存目錄有沒有 `_probe_persist.txt`
  - 有 → 印「找到上次執行留下的檔案，內容：<內容>」
  - 沒有 → 印「這是第一次執行，請再跑一次才知道答案」
  - 不論有無，最後都把目前時間寫進去，**這個檔不要刪**
- 列出目前工作目錄底下的檔案與資料夾（最多 30 個），我要知道使用者上傳的檔案會落在哪

**第 5 類：執行能力**
- `subprocess` 能不能用？跑 `python -c "print('ok')"` 試試，印結果或錯誤
- 能不能 import 並執行同目錄下的另一個 .py 檔？（寫一個小檔測試後刪掉）
- **輸出長度**：連續印 `[1]` 到 `[500]`（每 25 個換行），最後印「若你看得到 [500]，代表輸出未被截斷」
- 印出 `sys.getrecursionlimit()` 與可用記憶體（若 `resource` 模組可用）

**第 6 類：時間限制**
- 記錄開始時間，跑一個迴圈每秒印一次「已經過 N 秒」，印到 **30 秒**為止
- 最後印出實際經過的總秒數
- 如果你看到輸出停在中途，那就是平台的執行時間上限

**第 7 類：平台線索**
- 印出環境變數**總數**
- 找出名稱含 SPADE、PLATFORM、AGENT、SANDBOX、WORKSPACE、SKILL、RUNTIME、CONTAINER、K8S、POD、JOB、SESSION 字樣的變數，**只印名稱、不印值**，最多 25 個
- 印出目前使用者名稱（`getpass.getuser()`）與行程 ID

**輸出要求：**
- 執行腳本，把**原始輸出完整貼出來**放在程式碼區塊裡
- **不要只給摘要或結論**，我需要原始數據
- 某項因權限或沙箱限制失敗時，那個失敗訊息本身就是重要資訊，照實保留不要省略

## ── 結束複製 ──

---
---

# Part B：平台規格表

依 Part A 的結果，加上你自己知道的事，把下面填好。
**以後每次要我做 SPADE 相關的工作，把這一整份 Part B 貼給我就好。**

有些欄位 Part A 測不出來（標了「探測不到」），要你或資訊單位提供。
不確定的就寫「不確定」——**寫不確定比寫錯好**，我會據此保守設計或先問你。

## B1. 執行環境

```
Python 版本            ：
作業系統               ：
stdout 編碼            ：
中文顯示是否正常        ：
標準函式庫有被移除嗎    ：（有的話列出哪些）
```

## B2. 套件

```
可用的第三方套件        ：（列出 Part A 測到「有」的）
能不能自行安裝套件      ：能／不能／不確定
若能，走什麼管道        ：（內部套件庫？proxy？）
```

## B3. 檔案系統

```
使用者上傳的檔案放哪    ：（絕對路徑）
產出檔案該寫到哪        ：（絕對路徑）
可寫入的位置            ：
【關鍵】執行之間檔案保留嗎：保留／不保留／不確定
中文檔名可用嗎          ：
```

> **「檔案保留嗎」是所有問題裡最關鍵的一題。**
> 不保留的話，「第一步產出中間檔、第二步讀進來」這種多步驟設計完全不成立，
> 工具必須改成單次執行做完所有事。這會改變整個架構，不是調參數而已。

## B4. 執行限制

```
單次執行時間上限        ：（Part A 第 6 類可測出來）
stdout 會不會被截斷      ：（第 5 類可測出來）
subprocess 可用嗎        ：
記憶體限制              ：（探測不到，要問資訊單位）
能不能跑背景／長時間任務 ：（探測不到）
```

## B5. 網路

```
能連外網嗎              ：能／不能／限白名單
若限白名單，開放哪些     ：
內部服務可以連嗎        ：
```

## B6. Skill 機制（探測不到，全部要你提供）

```
Skill 定義檔格式        ：YAML frontmatter／獨立設定檔／平台介面填寫
Skill 放在哪            ：（絕對路徑或上傳方式）
資料夾命名有規則嗎      ：
Skill 怎麼被觸發        ：（靠 description 自動判斷？使用者指定？）
一個 skill 能有多少檔案 ：有無數量或大小上限
更新 skill 要重啟嗎      ：
```

## B7. 對話與 I/O（探測不到，全部要你提供）

```
使用者怎麼給輸入        ：上傳／給路徑／貼文字（可複選）
產出怎麼回到使用者      ：對話中顯示／使用者自己去資料夾拿／可下載
使用者進得去產出資料夾嗎 ：進得去／要請資訊單位／不確定
AI 看得到腳本完整輸出嗎  ：（還是也會被截斷）
對話有長度上限嗎        ：
```

> **「產出怎麼回到使用者」影響很大。**
> 如果使用者拿不到檔案，所有該讓他決定的內容都必須寫進對話文字裡，
> 工具的輸出設計會完全不同——不能只說「詳見 CSV」。

## B8. 合規與資安

```
可以處理公司內部文件嗎   ：
有沒有不能寫入的敏感目錄 ：
產出檔案會被留存多久     ：
有沒有稽核紀錄要求       ：
對外連線需要報備嗎       ：
```

---

## 我拿到 Part B 之後會做什麼

| 你填的內容 | 我會怎麼調整設計 |
|---|---|
| 檔案不保留 | 改成單次執行完成所有步驟，不產中間檔 |
| 缺某些套件 | 用標準函式庫自己實作，或明確標示該功能不可用 |
| subprocess 被擋 | 改成單一入口，不讓腳本互相呼叫 |
| stdout 會截斷 | 報表寫檔，對話只放摘要 |
| 使用者拿不到檔案 | 該決定的內容全部寫進對話，並訂出「什麼放對話、什麼留檔案」的分界 |
| 執行時間上限很短 | 拆步驟、加進度輸出，或改用更快的演算法 |
| 不能連外網 | 排除所有需要下載模型或查外部資料的功能 |

**不確定的欄位我不會自己猜。** 會保守設計，或在動工前先問你。
